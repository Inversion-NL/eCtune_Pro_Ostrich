Custom Tuned Maps:

When you have a calibration/bin you want to use as a tuned map.
You make in export of the map(these will export the fuel/ignition/ve tables)
The rpm/load scalars will also be exported but load scalar will be overwritten when using
this table import in the basemap creator

format
A.B.bin
A= Engine
B= is comment(including spaces)